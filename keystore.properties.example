## Release signing example.
## Copy to keystore.properties and fill with private values.
## Never commit keystore.properties or the keystore file.

storeFile=release-key.jks
storePassword=replace-with-private-store-password
keyAlias=astrax
keyPassword=replace-with-private-key-password
